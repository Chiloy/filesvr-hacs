from django.urls import path, re_path

from pam import views
from django.urls import path, include

from pam.forms import PamUserForm

urlpatterns = [
    # re_path('sftpuser/$', views.create_user),
]
